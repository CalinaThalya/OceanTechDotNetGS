@model IEnumerable<OceanTechDotNetGS.Models.RegistroVazamento>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.DataDetecao)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TipoSeveridade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescricaoVazamento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RegistroUsuario)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.DataDetecao)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TipoSeveridade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescricaoVazamento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RegistroUsuario.Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
